// TODO: add JSDoc to make the types this function expects clear, and to enable warnings in
// code editors if usage is incorrect.
function addUp(a, b) {
    return a + b;
}

console.log(addUp(1, 2));
console.log(addUp({}, 5));
console.log(addUp(true, false));
