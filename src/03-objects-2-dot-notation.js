/**
 * @param {Object} options
 * @param {string} options.orderBy - database field to order queries by
 * @param {number} options.skip
 * @returns {string}
 */
function getBooks({orderBy, skip}) {
    return 'some content';
}